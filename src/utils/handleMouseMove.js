export const handleMouseMove = (event) => {
  console.log(event);
  let mouseX = event.clientX; // получаем координату X мыши
  let mouseY = event.clientY; // получаем координату Y мыши

  // console.log(`Координаты мыши: x=${mouseX}, y=${mouseY} `);
};
